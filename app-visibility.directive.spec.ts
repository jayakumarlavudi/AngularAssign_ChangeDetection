import { AppVisibilityDirective } from './app-visibility.directive';

describe('AppVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new AppVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
