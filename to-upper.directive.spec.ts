import { ToUpperDirective } from './to-upper.directive';

describe('ToUpperDirective', () => {
  it('should create an instance', () => {
    const directive = new ToUpperDirective();
    expect(directive).toBeTruthy();
  });
});
